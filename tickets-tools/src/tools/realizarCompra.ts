// TODO (Pessoa 2): tool MCP — realizar_compra
// Argumentos: { intencao_id: string, metodo_pagamento: "cartao" | "pix" }
// Retorno: sucesso (Transacao) ou erro { status: "recusado", erro, mensagem }
