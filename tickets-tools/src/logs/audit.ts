// TODO (Pessoa 2, extra): log auditável de cada chamada de tool (quem, quando, quanto, resultado)
export function logToolCall(entry: unknown) {
  // console.log ou persistência simples em arquivo/memória
}
