// TODO (Pessoa 3): única conexão com a API do Gemini
// - monta o histórico completo da conversa a cada turno
// - registra as tools disponíveis (via src/mcp/adapter.ts) na chamada ao Gemini
// - executa a tool call retornada, chamando o MCP client (src/mcp/client.ts)
// - devolve o resultado da tool ao Gemini para gerar a resposta final
